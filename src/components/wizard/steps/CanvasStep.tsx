import React from 'react';
import { EnhancedCanvasStep } from './EnhancedCanvasStep';

// Alias for backwards compatibility
export const CanvasStep: React.FC = () => {
  return <EnhancedCanvasStep />;
};